import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";
import { site, phoneLink, whatsappLink } from "@/data/site";
export const metadata = { title: "Contact RSK Tours and Travels" };
export default function ContactPage() { return <div className="page-shell"><SiteHeader /><main className="container"><section className="page-hero"><p className="eyebrow">Let&apos;s talk about Goa</p><h1>One message, and we&apos;ll take it from there.</h1><p>For a taxi, airport transfer or tour plan, reach us directly. We keep the conversation clear and practical.</p></section><section className="section" style={{ paddingTop: 0 }}><div className="contact-grid"><article className="contact-card glass"><p className="eyebrow">Call or WhatsApp</p><h3>Quickest response</h3><p className="section-intro">For dates and availability, a message is the easiest way to reach the team.</p><p><a href={phoneLink()}>Call {site.phone}</a></p><p><a href={whatsappLink()}>WhatsApp {site.whatsapp}</a></p></article><article className="contact-card glass"><p className="eyebrow">Email</p><h3>For a considered plan</h3><p className="section-intro">Send your route, dates and group size to:</p><p><a href={`mailto:${site.email}`}>{site.email}</a></p><p className="section-intro">Hours: {site.hours}</p></article></div><div style={{ marginTop: "1rem" }}><iframe className="map" title="RSK Tours and Travels location" src={site.mapEmbed} loading="lazy" referrerPolicy="no-referrer-when-downgrade" /></div></section></main><SiteFooter /></div>; }
